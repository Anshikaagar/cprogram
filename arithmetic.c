#include <stdio.h>
void main()
{
    int a,b,sum,sub,mul,divi;
    scanf ("%d%d", &a,&b);
    sum=a+b;
    sub=a-b;
    mul=a*b;
    divi=a/b;
    printf ("Sum is:%d",sum);
    printf ("\nSubtration is: %d", sub);
    printf ("\nMultiplication is: %d", mul);
    printf ("\nDivision is: %d", divi);
}
